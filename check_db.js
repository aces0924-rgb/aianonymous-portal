const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const event = await prisma.event.findFirst(); console.log('FeatureFlags:', event.featureFlags); } main().catch(console.error).finally(() => prisma.$disconnect());
