const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const event = await prisma.event.findUnique({ where: { slug: 'aicomfes' }, include: { schedules: { orderBy: { order: 'asc' } }, rules: { orderBy: { order: 'asc' } }, faqs: { orderBy: { order: 'asc' } } } }); if (!event) { console.log('Event not found'); return; } console.log('TITLE:', event.title); console.log('DESCRIPTION:'); console.log(event.description); console.log('=== SCHEDULES ==='); event.schedules.forEach(s => console.log(s.date + ': ' + s.title)); console.log('=== RULES ==='); event.rules.forEach(r => console.log(r.title + '\\n' + r.content)); console.log('=== FAQS ==='); event.faqs.forEach(f => console.log('Q: ' + f.question + '\\nA: ' + f.answer)); } main().catch(console.error).finally(() => prisma.$disconnect());
